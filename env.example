# Firebase Configuration
# Copy this file to .env.local and fill in your values
# Both client and server runtimes read from these variables

# --- Production (default) client config ---
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=
NEXT_PUBLIC_FIREBASE_DATABASE_ID=(default)

# --- Optional staging client config (used when NEXT_PUBLIC_ENVIRONMENT=staging) ---
NEXT_PUBLIC_FIREBASE_STAGING_API_KEY=
NEXT_PUBLIC_FIREBASE_STAGING_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_STAGING_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STAGING_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_STAGING_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_STAGING_APP_ID=
NEXT_PUBLIC_FIREBASE_STAGING_MEASUREMENT_ID=
NEXT_PUBLIC_FIREBASE_STAGING_DATABASE_ID=(default)

# Firebase Admin SDK Configuration
# Option 1: Service account key fragments (recommended for local dev & CI)
# Provide the fields below (staging variants optional, only needed when using NEXT_PUBLIC_ENVIRONMENT=staging)
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# Optional extras (only if you have them)
# FIREBASE_ADMIN_CLIENT_ID=
# FIREBASE_ADMIN_PRIVATE_KEY_ID=

# Option 2: Service account key as JSON string
# Get this from Firebase Console → Project Settings → Service Accounts → Generate new private key
# Copy the entire JSON content as a single-line string (keep the single quotes)
# FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"centerforsportsandscience","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}'

# Option 2: Path to service account JSON file (recommended for local development)
# Download the service account key from Firebase Console and save it as firebase-service-account.json
# Then uncomment the line below:
# GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json

# Staging overrides (optional)
# FIREBASE_ADMIN_STAGING_PROJECT_ID=
# FIREBASE_ADMIN_STAGING_CLIENT_EMAIL=
# FIREBASE_ADMIN_STAGING_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# FIREBASE_SERVICE_ACCOUNT_KEY_STAGING='{"type":"service_account",...}'
# GOOGLE_APPLICATION_CREDENTIALS_STAGING=./firebase-service-account-staging.json

# Database ID
# (default) is used automatically. Override only if you created a named database.
# FIREBASE_DATABASE_ID=sixs-physio
# FIREBASE_STAGING_DATABASE_ID=sixs-physio

# Other Services (Optional)
# Resend Email Service
# RESEND_API_KEY=your_resend_api_key

# Twilio SMS & WhatsApp
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=+1234567890

# Sentry Error Tracking (optional)
# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn
# SENTRY_ORG=your_sentry_org
# SENTRY_PROJECT=your_sentry_project
# SENTRY_AUTH_TOKEN=your_sentry_auth_token


rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠ DEVELOPMENT RULES - Allows all reads/writes (NOT for production!)
    // Change these rules before deploying to production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}


